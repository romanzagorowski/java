/*
import model.Figure;
import model.Circle;
import model.Rectangle;
import model.Square;
import model.Square2;
import print.Printer;
import java.util.Arrays;
*/

public class App {
    public static void main(String[] args) {

        /*
        Circle circle = new Circle(2.0);
        Printer.printArea(circle);

        Rectangle rectangle = new Rectangle(3.2, 2.7);
        Printer.printArea(rectangle);

        Square square = new Square(2.3);
        Printer.printArea(square);

        Figure[] figures = new Figure[] { circle, rectangle, square };

        Printer.printArea(Arrays.asList(figures));
        */
    }
}

